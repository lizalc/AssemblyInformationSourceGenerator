# AssemblyInformationSourceGenerator (Not fully working)

Generator will generate the source for the attribute data but it is not accessible from the code for some reason. It _is_ accessible via reflection but that defeats the point.
